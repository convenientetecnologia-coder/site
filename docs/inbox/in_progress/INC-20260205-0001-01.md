### INC-20260205-0001-01 — P1 — Bootstrap do site + padrão definitivo de páginas

- **State**: `in_progress`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

#### Objetivo

Preparar `C:\site` para gerar ~200 páginas (3 por cidade) com:
- conteúdo único por cidade e por intenção,
- guardrails anti-spam,
- deploy “commit -> publish” via Hostinger Git Deploy.

#### Critério de aceite

- Estrutura canônica de docs pronta (`docs/*` + `docs/inbox/*`).
- Eleventy configurado com:
  - `npm run dev` funcionando
  - `npm run build` gerando `dist/`
  - `npm run validate` garantindo regras básicas (unique title/H1, mínimo palavras, 3 páginas por cidade)
- Templates das 3 intenções gerando URLs:
  - `/fretes-em-{cidade}`
  - `/mudancas-em-{cidade}`
  - `/frete-urgente-em-{cidade}`

