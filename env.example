# Exemplo (NUNCA versionar o arquivo .env com chave real)
#
# Windows PowerShell:
#   $env:OPENAI_API_KEY="SUA_CHAVE_AQUI"
#   $env:OPENAI_MODEL="gpt-5.2"
#
# Alternativa (recomendada neste projeto): copie este arquivo para `local.env`
# e preencha a chave lá (o `local.env` é ignorado pelo Git).
#
# Depois:
#   npm run neighborhoods:fetch -- --city "Florianópolis" --slug florianopolis
#

OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2
